package com.atguigu.chapter13

import scala.collection.mutable.ArrayBuffer

object Test02 {
  def main(args: Array[String]): Unit = {

//    def map(arr:Array[Int],op:Int=>Int)={
//      for(x <- arr) yield op(x)
//    }
//
//
//    val v2: Array[Int] = map(Array(1,2,3,4,5),_*2)
//    println(v2.mkString(","))

//    def filter(arr:Array[Int],op:Int=>Boolean)={
//      val buf = ArrayBuffer[Int]()
//      for(x <- arr if op(x)) buf += x
//      buf.toArray
//    }
//
//    println(filter(Array(1, 2, 3, 4, 5), _ % 2 == 1).mkString(","))

//    def reduce(arr:Array[Int],op:(Int,Int)=>Int)={
//      var init = arr(0)
//      for (x <- 1 until arr.length) init = op(init,arr(x))
//      init
//    }
//
//    println(reduce(Array(1, 2, 3, 4, 5), _ * _))
  }
}
