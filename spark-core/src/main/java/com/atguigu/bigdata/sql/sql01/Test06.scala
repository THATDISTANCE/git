package com.atguigu.bigdata.sql.sql01

import org.apache.hadoop.yarn.webapp.hamlet.HamletSpec.A

import scala.collection.immutable.HashMap
import scala.collection.{LinearSeq, SortedSet, mutable}

object Test06 {
  def main(args: Array[String]): Unit = {


    val xs = List(1, 2, 3, 4, 5)

//    val iterator: Iterator[List[Int]] = xs.grouped(3)
//    for (elem <- iterator) {
//      println(elem)
//    }

//    val iterator1: Iterator[List[Int]] = xs.sliding(3,2)
//
//    for (elem <- iterator1) {
//      println(elem)
//    }

//    val iterator: Iterator[Int] = xs.iterator
//    for (elem <- iterator) {
//      println(elem)
//    }

  }
}
